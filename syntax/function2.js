console.log(Math.round(1.6));
console.log(Math.round(1.4));

// 어떤 로직인지 모르지만 입력값을 받아서 처리해준다.

function sum(first , second) { // parameter 매개변수
    return first + second;
}
sum(2, 10); // 입력값 argument , 인자

console.log(sum(10, 40));

